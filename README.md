# Escape_Proj
Escape Room Computer project for grade 12

pip install the following

tkinter, pillow, mysql-connector-python,


for naman and devashri:

To update the repo follow this - 

First time only:
git clone https://github.com/Joel-Saji-ofo/Escape_Proj.git
cd Escape_Proj

After that, follow as routine:
git pull/ git pull origin main (second one only if the first one shows an error (it wont))
write whatever code you want to write
git add .
git commit -m "a comment about what you changed"
git push

if this routine isnt followed, the github repo wont be updated with your work

repo link : https://github.com/Joel-Saji-ofo/Escape_Proj
